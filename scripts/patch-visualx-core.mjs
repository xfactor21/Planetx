import { readFileSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
const appDir=process.argv[2];if(!appDir)throw new Error('Visual.X app directory required')
const appPath=join(appDir,'src','App.tsx');let app=readFileSync(appPath,'utf8')
const replace=(from,to,label)=>{if(!app.includes(from))throw new Error(`Visual.X patch missing: ${label}`);app=app.replace(from,to)}
replace("import{api}from'@appdeploy/client'","import{api}from'./catalogClient'",'catalog client import')
replace("type CatalogTrack={id:string;title:string;artist:string;bpm:number|null;duration:number|null;isPreviewOnly:boolean;image:string|null};","type CatalogTrack={id:string;title:string;artist:string;bpm:number|null;duration:number|null;isPreviewOnly:boolean;image:string|null};type FeaturedTrack={id:string;title:string;url:string};const featuredTracks:FeaturedTrack[]=[{id:'digital-decay',title:'Digital Decay',url:'/music-tracks/digital-decay.mp3'},{id:'glitch-god',title:'Glitch God',url:'/music-tracks/glitch-god.mp3'},{id:'ghost-in-the-machine',title:'Ghost in the Machine',url:'/music-tracks/ghost-in-the-machine.mp3'}];",'featured tracks')
replace("[showOverlay,setShowOverlay]=useState(false),[fullscreen,setFullscreen]=useState(false);","[showOverlay,setShowOverlay]=useState(false),[fullscreen,setFullscreen]=useState(false),[entryOpen,setEntryOpen]=useState(true),[entryPanel,setEntryPanel]=useState<'home'|'search'|'featured'>('home');",'entry modal state')
replace("const adoptFile=async(file:File,label?:string,knownBpm?:number|null)=>{const run=++analysisRunRef.current;","const adoptFile=async(file:File,label?:string,knownBpm?:number|null)=>{const run=++analysisRunRef.current;setReady(false);",'analysis readiness start')
replace("setDna(result);setDuration(result.duration);setProgress(1);setStatus(`Ready · ${result.sections.length} sections · ${result.bpm} BPM · ${Math.round(result.tempoConfidence*100)}% tempo confidence`);return true","setDna(result);setDuration(result.duration);setProgress(1);setReady(Boolean(audioRef.current&&audioRef.current.readyState>=2));setStatus(`Ready · ${result.sections.length} sections · ${result.bpm} BPM · ${Math.round(result.tempoConfidence*100)}% tempo confidence`);return true",'analysis readiness finish')
replace("try{await adoptFile(file)}catch","try{if(await adoptFile(file))setEntryOpen(false)}catch",'upload closes entry')
replace("await adoptFile(file,`${t.title} — ${t.artist}`,t.bpm)}catch","if(await adoptFile(file,`${t.title} — ${t.artist}`,t.bpm))setEntryOpen(false)}catch",'search closes entry')
replace("const toggle=async()=>{const a=audioRef.current;if(!track||!a)return;","const chooseFeaturedTrack=async(t:FeaturedTrack)=>{const run=++trackLoadRunRef.current;analysisRunRef.current++;setLoadingTrack(t.id);setCatalogMessage('');try{const res=await fetch(t.url);if(run!==trackLoadRunRef.current)return;if(!res.ok)throw new Error(`Audio download failed (${res.status}).`);const blob=await res.blob();if(run!==trackLoadRunRef.current)return;const file=new File([blob],`${t.title}.mp3`,{type:blob.type||'audio/mpeg'});if(await adoptFile(file,`${t.title} — xFactor`))setEntryOpen(false)}catch(e:unknown){if(run===trackLoadRunRef.current)setCatalogMessage(e instanceof Error?e.message:'Could not load this xFactor track.')}finally{if(run===trackLoadRunRef.current)setLoadingTrack(null)}};const toggle=async()=>{const a=audioRef.current;if(!track||!a||!ready||!dna||progress<1)return;",'featured loader and guarded toggle')
replace("onCanPlay={()=>setReady(true)}","onCanPlay={()=>{if(dna&&progress>=1)setReady(true)}}",'canplay waits for analysis')
replace("<img src='/resources/new-transparent-visual-x-wordmark-asset.png'","<img src='./resources/new-transparent-visual-x-wordmark-asset.png'",'wordmark path')
writeFileSync(appPath,app)
console.log('Patched Visual.X core behavior')
